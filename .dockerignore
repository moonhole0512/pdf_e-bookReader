# Python 가상 환경
venv/
.venv/
_testcode/

# Git 관련 파일
.git/

# Python 캐시 파일
__pycache__/
*.pyc

# 로컬 환경 변수 파일 (컨테이너 내부에서 별도로 설정)
.env

# Windows 전용 실행 스크립트
run.bat

# 데이터베이스 파일이 포함된 instance 디렉터리 (볼륨으로 마운트할 예정)
instance/

# PDF 파일이 포함된 pdfs 디렉터리 (볼륨으로 마운트할 예정)
pdfs/